# Skillkit Share Server Data

This directory stores shared skill packages and metadata.

## Contents

- `*.json` - Share metadata files
- `*.zip` - Compressed skill packages

## Notes

- All files in this directory are auto-generated by the share server
- Files are automatically cleaned up after 7 days (expiration)
- This directory is excluded from git via `.gitignore`
